[behave]
# Path to feature files
paths = ../../features

# Step definitions and environment location
# Behave will look for steps/ and environment.py in the working directory

# Output format
format = progress
stdout_capture = false
stderr_capture = false
log_capture = false

# Show snippets for undefined steps
show_snippets = true

# Color output
color = true
